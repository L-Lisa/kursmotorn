# DECISIONS — Kursmotorn-bygget

> Varje val som inte står i SPEC/datamodell loggas här med datum + motiv (CLAUDE.md #7).
> Grindkvittenser loggas också här.

## 2026-07-12 — Fas 0: verktygsval vid scaffold
- **Next.js 16.2.10 (App Router) + React 19 + Tailwind 4** — `create-next-app@latest` med `--src-dir`, import-alias `@/*`. Motiv: Lisas beprövade mönster (Jobbresan kör Next 16); senaste stabila.
- **shadcn/ui init: `-b radix`, preset `nova`, css-variables.** Motiv: radix = etablerade primitives (Jobbresan-mönstret); nova-presetens palett är bara startvärde — motor-chromens tokens ur `../brand.md` ersätter den i fas 2. Nya CLI:t (v4.x) har inte kvar `--base-color`-flaggan.
- **AGENTS.md (autogenererad av create-next-app) behållen** — generisk Next 16-vägledning. Vid konflikt vinner repo-CLAUDE.md (dess uttryckliga regel).
- **Supabase-projekt:** `kursmotorn`, ref `lsqfsntwnvquakmqeryn`, eu-north-1, skapat 2026-07-12 via MCP med Lisas stående OK (10 USD/mån — kostnaden har startat, Lisa meddelad i sessionen).
- **Git:** nested repo i `projects/kursmotorn/app/` (repo-roten per byggspec-paketet); yttre COWORK-repot spårar inte hit in. Författare verifierad FÖRE första commit: `Lisa <lisaojeland@gmail.com>` (global config — Vercel-författarlärdomen 2026-07-05).
- **Env-namn `SUPABASE_SECRET_KEY` + `NEXT_PUBLIC_SUPABASE_ANON_KEY`:** app-koden följer Supabases nyckelterminologi. RLS-policies skrivs mot `authenticated`/`anon`; serverfunktioner som måste kringgå RLS (utfärdande, loggskrivning) körs `SECURITY DEFINER`. Migrationerna nämner Postgres-rollen `service_role` legitimt i GRANT/policy-rader — därför är hemlighetsgrinden värdeorienterad (se GRIND 0-beslutet nedan), inte ordbaserad.
- **Modellnot:** fas 0 påbörjad av Fable 5; Lisa bytte till **Opus 4.8** efter uppsättningen → resten av bygget följer `system/model-notes.md`-protokollet (mer mekaniskt, verifiera oftare, färsk verifieringssubagent på high-stakes-faser, eskalera hellre än gissa).

## 2026-07-12 — GRIND 0 kvitto
- **repo bygger lokalt:** `npm run build` grönt (`/` + `/_not-found` prerendered). ✓
- **`.env*` git-ignorerad:** `.env`, `.env.local`, `.env.production` fångas av `.gitignore`; `.env.example` (endast tomma värden) är den enda spårade env-filen. ✓
- **författare:** `Lisa <lisaojeland@gmail.com>` verifierad på HEAD FÖRE (och efter amend) allt arbete. ✓
- **hemligheter = 0 (binär grind, pass).** Se beslutet nedan.

### Beslut: hemlighetsgrinden värdeorienterad (Lisas order 2026-07-12)
Den ursprungliga grinden (`grep -ci` på ett rått ordmönster) hade två fel: (1) mönstertexten matchade sig själv där grinden citeras i spec-filerna → falska träffar utan att någon hemlighet läckt; (2) den skulle dessutom falsklarma på **legitim `service_role`-SQL i RLS-migrationerna** — GRANT- och policy-rader nämner Postgres-rollen med rätta. Ett kortlivat "lämna som känd artefakt"-utfall revs upp: en säkerhetsgrind ska vara **binär** (0 = pass, allt annat = stopp).

**Fix:** grindens kommando byttes i `TASKS.md` (GRIND 0), `ACCEPTANCE_CRITERIA.md` (§Genomgående) och `CLAUDE.md` (§Git) till att matcha faktiska hemlighets*värden* och utesluta lockfilen:
```
git log -p -- . ':(exclude)package-lock.json' | grep -cE "sk-ant-[A-Za-z0-9]|sb_secret_[A-Za-z0-9]|eyJ[A-Za-z0-9_-]{20,}"
```
Mönstret träffar en riktig Anthropic-nyckel, en Supabase-secret och en JWT, men varken sin egen regex-text (hakparenteserna bryter självträffen) eller ordet `service_role` i SQL. `package-lock.json` utesluts så integrity-hasharnas `eyJ…`-slump inte räknas.

**Historik:** repot var nytt (en commit) → fas 0-commiten amend:ades (+ `reflog expire`/`gc`) så tidigare klartext-markörer försvann ur den nåbara historiken. **Verifierat: kommandot ger exakt `0`.** Grinden körs oförändrad vid varje efterföljande fas.

### Incident: repo-CLAUDE.md klobbrades av scaffold-rsyncen (åtgärdad samma commit)
`create-next-app` genererar en egen `CLAUDE.md` som bara innehåller `@AGENTS.md`. När scaffolden `rsync`:ades in i `app/` skrev den över byggspecens repo-regelfil (`CLAUDE.md`, 5,5 kB) — och den stubben (11 B) hann committas i fas 0. Upptäckt när §Git-editen slog fel ("shorter than offset"). **Åtgärd:** byggspecens `CLAUDE.md` återställd ordagrant ur sessionskontexten (lästes vid sessionsstart), med `@AGENTS.md`-importen tillagd sist så Next 16-vägledningen behålls; repo-reglerna vinner vid konflikt. Ingår i den amend:ade fas 0-commiten. **Lärdom (till fas 9 + projektminnet):** scaffold-verktyg kan generera egna `CLAUDE.md`/`AGENTS.md` — rsynca ALDRIG rått över en repo-rot som redan bär byggspec-filer; exkludera `CLAUDE.md`/`AGENTS.md` eller diffa efter kopiering.

## 2026-07-12 — Fas 1: datamodell + RLS + seed
- **Migrationer (supabase/migrations/):** `..01_schema` (24 tabeller ur datamodell.md + guide-spec §2; tenant_id på ALLT; partiellt unikt index `activity_logs_practice_day_unique`; partiellt unikt `enrollments_one_active_per_course` som backar server-regeln på DB-nivå), `..02_rls` (3 SECURITY DEFINER-hjälpfn + RLS på alla 24 + policies), `..03_storage` (privat bucket `recordings` + path-prefix-policies `<tenant>/<user>/<section>`), `..04_hardening` (search_path-fix). **Applicerade på fjärrprojektet via MCP** (tomt projekt = provisionering; samma SQL som filerna).
- **Beslut — enum vs CHECK:** alla värdemängder som `text` + `CHECK` (lättare att evolvera än Postgres-enum).
- **Beslut — `enrollments.course_id` denormaliserad:** krävs för det partiella unika indexet "en aktiv enrollment per user+kurs" (kurs nås annars bara via cohort). Serverfunktionen i fas 6 håller det konsistent med cohort.course_id.
- **Beslut — RLS-mönster:** hjälpfn är SECURITY DEFINER (ägs av postgres → kringgår RLS på memberships → ingen policy-rekursion). Config/innehåll = medlem läser, admin skriver. Deltagardata = egen rad (`user_id = auth.uid()` i USING + WITH CHECK), admin ser/hanterar tenantens. Admin-hanterad deltagardata (enrollments/certificates/approvals/mg_guide_status) = ingen deltagar-insert. `certificates` privat i fas 1 — publika `/verify`-sidan får egen SECURITY DEFINER-läsfunktion i fas 5 (inte öppen RLS).
- **Advisor-WARN som ACCEPTERAS (dokumenteras, ej åtgärdas i v1):** de tre hjälpfn:erna är RPC-exponerade för anon/authenticated. Ofarligt — varje funktion avslöjar bara ANROPARENS egen status, aldrig annans, och RLS kräver att de är anropbara. `auth_leaked_password_protection` (HaveIBeenPwned) avstängt = auth-konfig till go-live-grinden (riktiga lösenord), inte v1 med fiktiva konton.
- **Seed (supabase/seed.sql):** ENDAST fiktiva deltagare — 2 tenants, 5 login-kapabla konton (auth.users + auth.identities, lösenord `Testlosen123!`), Lisa = plattformsadmin + owner båda, en tenant1-admin som INTE är plattformsadmin (för admin-isolationstestet), Anna/Bengt (tenant1), Cecilia (tenant2) + kurser/moduler/sektioner/prov/kohorter/enrollments/loggar/certifikat. Login-konton via direkt SQL (ingen secret key behövdes).

### GRIND 1 kvitto — RLS-isolationssviten (7/7 PASS)
`npm run test:rls` (node:test + supabase-js, riktiga inloggningar): 1. anon kan inte läsa kurser · 2. Anna ser bara egen tenant + egen data · 3. Bengt ser inte Annas försök · 4. Anna kan inte skapa försök i Bengts namn (WITH CHECK) · 5. tenant1-admin (ej plattformsadmin) ser tenant1 aldrig tenant2 · 6. Cecilia ser bara tenant2 · 7. `practice_day` dagsunik, `guide_session` ej.
- **Node 20-fix:** supabase-js realtime kräver WebSocket som Node 20 saknar → `ws` injiceras som `realtime.transport` (dev-dep). Bekräftar minnesnoten "supabase-js kräver Node 22"; PostgREST-vägen funkar på 20 med ws-shim.
- **Hemlighetsgrind fortsatt 0** (`.env.local` med anon-JWT:n är git-ignorerad).
- **RESIDUAL (ej blockerande):** lokal `supabase db reset` kunde inte köras (ingen Docker i miljön) → reproducerbarhet verifierad via lyckad fjärrapplicering + passerande RLS-svit mot det applicerade schemat, inte via ren lokal reset. Kör i Docker-miljö före go-live (ACCEPTANCE fas 0–1).

## 2026-07-12 — Adversariell review av fas 0–1 (Opus, före fas 2) + två härdningar
Lisa bad om en granskning av fundamentet före fas 2. Grindarna omkördes skarpt mot live-DB (inte från loggen): **RLS-svit 7/7**, RLS på 24/24 tabeller, 0 tabeller utan policy, 55 policies, 3 SECURITY DEFINER-fn:er alla `search_path=public`, hemlighetsgrind 0, `.env*` ospårad, inga hårdkodade brand/priser i `src/`, säkerhetsadvisors = enbart de 6 accepterade + leaked-password (→ go-live). Fundamentet bedömt **solitt** — fynden nedan är härdnings-/täckningsluckor, inga brott. Två åtgärdade nu; fyra loggade som öppna.

**Fynd 1 (åtgärdat) — RLS-regressionsvakten testade nästan bara läsning.** `test/rls.test.mjs` utökad från 7 → 12 fall med negativa skrivvägar: deltagare kan inte skriva config (courses/sections), inte höja egen membership-roll, inte skapa upload/attempt i annans namn eller radera annans progress; tenant1-admin kan inte skriva in i tenant2 (cross-tenant WITH CHECK); **storage path-prefix-policyn testas nu skarpt** (egen mapp OK, annans nekad — tidigare 0 % täckt). Regel: INSERT-nekande ger error, UPDATE/DELETE-nekande via USING ger 0 rader — sviten kollar rätt sak per verb. **12/12 grönt.**

**Fynd 2 (åtgärdat) — `enrollments.course_id` denormaliserad utan DB-garanti.** Kunde driva från `cohorts.course_id` (RLS tillåter admin-skrivning) → "en aktiv per kurs"-indexet + certfönstren hade räknat tyst fel. Migration `..05_enrollment_integrity.sql`: `cohorts unique(id, course_id)` + composite-FK `enrollments(cohort_id, course_id) → cohorts(id, course_id) on delete cascade`. **Applicerad mot fjärr-DB på Lisas OK (`apply_migration`); bett verifierat** (mismatchad insert avvisas med foreign_key_violation; 3 befintliga enrollments intakta). DB:n garanterar nu det fas 6-serverfunktionen annars bara lovade.

**Öppna fynd (loggade, ej åtgärdade — Lisas beslut: fixa 1+2 nu):**
- **Fynd 3 [Medium, känd]:** `supabase db reset` + seed aldrig körd rent (ingen Docker) — uttryckligt Fas 0–1-kriterium. Live-schemat styrker filerna men är inte en ren reset. **Håll ACCEPTANCE fas 0–1 synligt öppen tills körd i Docker; blockerande före eject (fas 8)/go-live.**
- **Fynd 4 [Låg–Medium, framåtvakt]:** anon har noll läsning på `tenants`/`tenant_brands`. Fas 2:s publika tenant-brandade landnings-/login-sida + `/verify/<slug>` (fas 5) kräver en **smal SECURITY DEFINER-lookup (slug + brand-tokens)** — luckra ALDRIG upp RLS med bred anon-SELECT.
- **Fynd 5 [Låg]:** `practice_day`-unikheten är global per user (user_id, log_type, logged_date), inte per kurs — bekräfta avsikt före fas 7:s MG-fönster (deltagare i två kurser skulle annars blockeras).
- **Fynd 6 [Låg]:** endast `recordings`-bucketen finns; bild-/kursmedia-buckets + path-prefix-policyer byggs i fas 2/3 med samma rigor.

## 2026-07-12 — Fas 2: context-lager + tenant-rendering (commit `8ca0bbf`)

### Arkitekturbeslut
- **Eject-snittet realiserat som `src/lib/tenant/`.** All tenant-data läses genom `getTenantContext(slug)` → `parseBrand()` (enda stället en rå brand_spec tolkas). Brand-tokens blir inline `--t-*`-CSS-variabler via `brandCssVars()`, satta på tenant-layoutens wrapper. Komponenterna läser bara `var(--t-*)` — ingen komponent känner till en specifik tenant. Det är detta som gör exporten (fas 8) möjlig: byt datakällan, behåll komponenterna.
- **@supabase/ssr + `proxy.ts` (Next 16).** `middleware` heter `proxy` från v16 (Node-runtime default). Tre klienter: `server.ts` (RSC, anon-nyckel + session ur cookies), `client.ts` (browser, för login), `proxy.ts` (sessionsförnyelse; ingen auktoriseringslogik — RLS + per-route-kontroller gatar). Motorn rör **aldrig** service-nyckeln i renderingsvägen — tenant-isolationen vilar på RLS hela vägen.
- **Fynd 4 åtgärdat — smal publik brand-lookup.** Migration `..06`: `tenant_public_brand(slug)` SECURITY DEFINER returnerar EN tenants slug + brand_spec, `grant execute … to anon`. Ingen bred anon-SELECT på tenants/tenant_brands. Samma funktion används av login-/landningssidan (anon) OCH av inloggade vyer — ett context-lager oavsett rollen.
- **Routning: path-baserad `/{tenant-slug}/…`** i demon. Host-/subdomän-mappning kräver wildcard-DNS + Vercel-domän → **fortsatt öppen** (SPEC §5), avgörs mot Vercel-uppsättningen; path-vägen räcker för v1-demon och exporten.
- **Motor-namnet = konfigvariabel.** `NEXT_PUBLIC_APP_NAME` (default "Kursmotorn") i `src/lib/config.ts` — namnbytes-säkert.
- **Fonter self-hostas via `next/font/google`** (Inter, JetBrains Mono, Lora, Fraunces, Source Sans 3, Geist Mono). Namn i brand_spec → CSS-variabel via en registry. **v1-begränsning:** next/font kräver statiska importer, så en tenant kan bara välja bland de laddade familjerna; okänt fontnamn → sans-fallback. Ny kundfont = en importrad + deploy (självbetjäning senare). Loggat som medvetet avsteg.
- **MG `primary_dark` = `#0F1B30`** är en härledd mörkare Ink-nyans (MG:s brand.md anger inget primary_dark; Ink `#1A2942` är primär). Används bara på certifikatytor (fas 5). [Gissning] — bekräftas när certifikatet byggs.

### GRIND 2 kvitto (skärmdumpsloop + WCAG)
- **Skärmdumpar:** `outputs/kursmotorn/grind2/` (01 motor-login · 02–03 publika landningar · 04 motor-admin · 05–06 kursvyn i BÅDA tenants). Harness: `scripts/grind2-screenshots.mjs` (playwright, dev-dep — projektets stående skärmdumps-grind). **Samma komponent, två varumärken:** Andningskursen (sand/petrol/Lora/JetBrains) vs Mindfulnessguiden (Vellum/Ink-navy/Fraunces/Geist Mono) — MG:s tokens återgivna exakt mot Editorial Lugn. Motor-admin matchar "Varma maskinrummet"-styleprovet.
- **Acceptans #2 bevisad skarpt:** ändrade `andningskursen` primary i DB (#1F5F5B→#0B5563), `--t-primary` i renderad publik HTML följde med **utan kodändring, deploy eller omstart**; återställd exakt till #1F5F5B.
- **WCAG AA (script, kontrastberäkning):** motor + MG passerar allt (brödtext 13–15:1, muted 4.6–5.6:1, knappar 7–15:1). Andningskursen: brödtext 12.3:1, vit/petrol-knapp 7.4:1 = OK.
  - **Åtgärdat:** "Inspelning"-etiketten var accent-guld som text (2.99:1, FAIL) → gjord till **soft badge** (`--t-soft`-bg + `--t-primary-dark`-text), 8.9:1 (andning) / 14.2:1 (MG). Guld/mässing används inte längre som brödtext någonstans — matchar brand.md:s "accent = badges/detaljer, sparsamt".
  - **BESLUT (Lisa 2026-07-12): (a) acceptera 4.03.** Andning `muted #6E7A76` på bg `#F6F3ED` = **4.03:1** (0.47 under AA för liten sekundärtext, eyebrow-etiketter + modul-intro) accepteras — låst demo-brand-token (palett A, graderad i moodboard-rundan); MG:s muted #5A6273 = 5.58 passerar. Ändras inte utan ny gradering. MG mässing `#8B6F3F` på bg = 4.31:1 används bara som liten facilitations-etikett; badge-mönstret (soft-bg + primary_dark-text) löser det om/när det används som text.

### Standing regressionsvakter (grind före commit)
- RLS-sviten **12/12** grön mot live-DB · hemlighetsgrind **0** · build grön · namnbytes-grep (`mindfulnessguiden|andningskursen|respira` i `src/`) = **0** · inga priser/procent i `src/`.

### Öppet in i fas 3
- Host-/subdomän-routning (öppen sedan fas 0) · fynd 5 (practice_day-unikhet, före fas 7) · fynd 6 (bild-/kursmedia-buckets — byggs i fas 3 med kursimporten) · WCAG-punkten LÖST (Lisa accepterade 4.03).

## 2026-07-12 — Fynd 3 STÄNGT + lokal Docker-dev-miljö
- **`supabase db reset` körd rent i lokal Docker-stack** (Lisa startade Docker Desktop): alla sex migrationer (01–06) applicerade från noll + `seed.sql` inläst utan fel. Verifierat: 5 `.test`-konton, 2 tenants/brands/courses, `tenant_public_brand()` returnerar rad. **Fynd 3 (ACCEPTANCE fas 0–1: "supabase db reset + seed går igenom rent") är därmed uppfyllt** — reproducerbarheten bevisad mot ren DB, inte bara mot en applicerad fjärr.
- **Arbetsmodell fas 3+:** lokal Docker-DB = gratis scratchpad för iterering (import, gating); det betalda fjärrprojektet (`lsqfsntwnvquakmqeryn`, €25/mån Pro) rörs bara när en fas är solid, på Lisas OK. Lokalt begränsar inget vi bygger (ingen hosted-only-funktion i scope).
- **Namngivning (Lisas instruktion):** `config.toml` `project_id` "app" → **"kursmotorn"** så Docker-containrarna heter `supabase_*_kursmotorn` (inte det intetsägande `_app`). Regel framåt: namnge alltid resurser Lisa ser i en UI beskrivande.

## 2026-07-12 — Fas 3: kursimport + kursvy + gating (commits `a72121e`, `d390d1b`, `9bffd7e`)

### Arkitektur
- **Seed/import-uppdelning (löser fixtur-vs-innehåll utan brygghack).** `seed.sql` ger bara scaffolding (tenants/användare/kurser/kohorter/enrollments/villkor). Kursinnehållet (moduler/sektioner/prov) importeras ordagrant ur `../kurs/modul-N.md` via `npm run import` → **in i den befintliga kursen `1c000000`** (RLS-testet kräver "exakt en kurs per tenant"). Deltagardata seedas inte alls; RLS-sviten seedar sina egna rader och städar upp (content-agnostisk). Motiv: importen får äga innehållet fritt utan att spröda fixturer bryts — mjukare testning per Lisas instruktion 2026-07-12.
- **Parsern (`scripts/lib/parse-course.mjs`)** hanterar tre provformat i kursen (`**Fråga N.**`, `**N. text**`, `✔`-only utan Rätt svar-rad) + slutprovet (modul 9 §9.4). **Modul→sektion-mappning (SPEC §2.2):** för varje 🎥-modul (3,4,5,6,9) läggs en **dedikerad uppladdningssektion sist** (`upload_required:true`) — så blir "modulen låser nästa" = sista sektionens krav. Narrativa "Din inlämning"-sektioner (5,6) behålls som innehåll. Källkommentarer (`<!-- källa -->`) strippas som byggmetadata; prosan är ordagrann (verbatim-kontroll 65/65).
- **Prov-mappning:** per-modul-prov importeras som `is_final=false` (självkoll, gatar inte — kursplanen låter bara 🎥-uppladdningar låsa nästa modul). Modul 9:s slutprov = `is_final=true` (cert-villkoret `final_quiz_pass`). Provregelns quiz-gating testas via gating-kärnan (enhetstest), inte genom att tvinga awkward prov-krav på breathworks-sektioner.
- **Gating som REN TS-funktion** (`src/lib/tenant/gating.ts`), anropad server-side i RSC (kursvyn) OCH i checkoff-serveråtgärden (`actions.ts`). Uppfyller "gating = serverfunktion, aldrig klientlogik": klienten visar bara tillstånd; en låst sektion kan aldrig bockas av (enforce i action). Valde TS-server-fn framför plpgsql för enkelhet — logiken bor på ETT ställe och delas.
- **Lokal Docker = dev-sanning för fas 3+.** Fjärrprojektet (`lsqfsntwnvquakmqeryn`) håller fas 0–2-läget; synkas (seed-trim + import) vid nästa visningsbara milstolpe på Lisas stående push-OK (efter review/loop). Lokala nycklar i `.env.development.local` (gitignorerad). `tsx` (dev-dep) kör TS-tester.

### GRIND 3 kvitto
- **`npm run test:gating` 5/5:** self_paced sekventiell upplåsning · scheduled drip dag 7 · kombinerade villkor (checkoff+prov+upload kräver alla tre) · **provregeln i BÅDA lägen** (tid/avbockning låser aldrig upp förbi underkänt prov).
- **Riktiga appen (loop):** inloggad som Anna → kursvyn visar 9 moduler ordagrant, sektion 1 öppen, resten låsta (self_paced); avbockning av sektion 1 öppnade **exakt** nästa (64→63 låsta). Skärmdumpar `outputs/kursmotorn/grind3/`.
- **RLS-sviten omskriven** (self-seedande, content-agnostisk) **10/10** — tenant-isolation + negativa skrivvägar + storage-prefix + practice_day-unikhet, utan hårdkodade innehålls-UUID:n. Standing regressionsvakt intakt.
- build grön · hemlighetsgrind 0 · namnbytes-grep 0.

### Öppet in i fas 4
- Prov-motorn (ta prov, rätta, 80 %-tröskel, omtag) — då blir per-modul-proven interaktiva och slutprovet gatar certifikatet (fas 5). · MP4-uppladdning (TUS, stora filer) — då blir 🎥-sektionernas `upload_required` uppfyllbart i UI · media-buckets (fynd 6) byggs med uppladdningen · host-routning (SPEC §5) fortsatt öppen · fjärr-DB-synk vid milstolpe.

## 2026-07-12 — Fas 4: prov-motor + MP4/TUS-uppladdning (commits `1861204`, `ccb74f1`)

### Prov-motorn (facit-säker) — migration `..07`
- **Provintegritet som säkerhetskrav, inte bara UX.** Deltagaren får ALDRIG läsa `correct_index`/`explanation`, och får inte själv skriva `quiz_attempts` (annars kunde `passed=true` fejkas → kringgå certkravet i fas 5). Lösning: `qq_read` → **admin-only**; `qa_insert_own` **borttagen**. Deltagaren läser frågor via `get_quiz()` (SECURITY DEFINER, utan facit) och rättas i `submit_quiz_attempt()` (SECURITY DEFINER, läser facit server-side, respekterar `max_attempts` + `pass_threshold`, skapar attempt). Admin behåller full åtkomst (rättning/nollställning via `qa_admin`).
- **Provmappning bekräftad:** per-modul-prov = `is_final=false` (självkoll, gatar inte); slutprovet (modul 9) = `is_final=true` → cert-villkoret `final_quiz_pass` (fas 5). Verifierat: facit läcker ej, direktläsning av `quiz_questions` ger 0 rader för deltagare, scoring 100 %/75 %, `max_attempts=3` avvisar 4:e försöket, fejkad direkt-insert nekas.
- UI: `/[tenant]/kurs/prov/[quizId]` (frågor via `get_quiz`, inlämning via server-action → `submit_quiz_attempt`), resultat + omtag, prov-block i kursvyn. Facit stannar server-side hela vägen.

### MP4/TUS-uppladdning — migration `..08` + config
- **recordings-bucket:** `file_size_limit` höjt till **2 GB** + `allowed_mime_types` = video (migration `..08`); lokal `config.toml [storage] file_size_limit` → `2GiB`. Motorns marknadssärskiljare (benchmarken: konkurrenter kapar vid 10–100 MB).
- **`UploadControl`** (klient, `tus-js-client`, 6 MB-chunkar, resumable) laddar till path `<tenant>/<user>/<section>/<fil>`; storage-RLS (migration `..03`) gatar prefixet. Vid klar uppladdning → `recordUpload`-åtgärd (gating enforce:as: bara upplåst 🎥-sektion + rätt prefix) skapar `uploads`-raden → sektionen klar → nästa modul öppnas (samma gating-kärna).
- **Skarp verifiering (SPEC §5, "kunde inte testas i Cowork"):** 1 GB laddades upp via TUS på **26 s** (1024 MB-objekt bekräftat i storage); uppladdning till annans prefix **NEKAD** av RLS. Signerade URL:er för återuppspelning = fas 5 (verify/admin-visning).

### GRIND 4 kvitto
- Prov: scoring 100/75, `max_attempts` avvisar, facit-säkert, cheat-insert nekad · TUS 1 GB + path-prefix-isolation · upload→sektion-klar→nästa (gating enhetstestad) · **RLS-sviten omskriven för nya skrivvägar (försök via funktion, facit gömt) 10/10** · test:gating 5/5 · db reset + import rent · build grön · hemlighetsgrind 0.

### Öppet in i fas 5
- Certifiering: `course_certificate_requirements` + utfärdande-serverfunktion (läser typade krav: sections_complete + final_quiz_pass + attestation) · attestation (heder-och-samvete, versionerad) · certifikat-PDF ur tenant-brand (verktygsval → DECISIONS, å/ä/ö-test) + publik `/verify/<slug>` · signerade URL:er för uppladdad media · **breathworks-regressionen blir grind från fas 5**. Fjärr-DB-synk vid visningsbar milstolpe (Lisas push-OK).

## 2026-07-12 — Fas 5: Certifiering (breathworks-beviset) — migration `..09`

### Utfärdande som säkerhetskrav (samma klass som prov-motorn)
- **`issue_certificate()` (SECURITY DEFINER) re-verifierar ALLA typade krav server-side** innan certifikatraden skapas. `certificates`-RLS tillåter bara admin att skriva (`cert_write`) → deltagaren kan aldrig själv-inserta ett certifikat (regressionstestat: direkt-insert NEKAD). Serveråtgärden/UI:t som anropar funktionen är bara UX — utfärdandelogiken kan inte kringgås. Idempotent: befintligt icke-revokerat certifikat återanvänds (samma slug).
- **Kravutvärderingen bor i EN funktion** (`_cert_requirement_met`) som både status- (`certificate_status`) och utfärdandevägen anropar → samma sanning på ett ställe. **`sections_complete`-predikatet är ordningsfritt** (en sektion är "klar" när dess egna villkor är uppfyllda: checkoff→progress, quiz→godkänt försök, upload→uppladdning) och kan därför ligga i plpgsql utan att divergera från `gating.ts` sekvenslogik (som avgör upplåsnings-ORDNING, inte completion). **Fail-closed:** okända/ännu-ej-byggda kravtyper (`upload_sections`, `log_threshold` = MG fas 7) ger `false` → inget MG-certifikat utfärdas på tomhet.
- **Adversariellt fynd (åtgärdat i samma migration):** `_cert_requirement_met` var trots `revoke … from public` fortfarande anropbar av `authenticated` — Supabase grantar default execute till `anon`+`authenticated` på nya public-funktioner. En deltagare kunde ha probeat en ANNAN användares progress (funktionen är SECURITY DEFINER, kringgår RLS). **Fix:** `revoke execute … from public, anon, authenticated` (definer-funktionerna äger anropet som postgres och påverkas inte). Verifierat: direktanrop ger nu `42501`; `certificate_status` för annan user ger `null` (kräver egen uid eller admin).

### Attestation (heder-och-samvete) — versionerad, server-styrd lydelse
- **Lydelsen bor i `attestation_statement(type)`** som BÅDE förhandsvisningen i UI:t och `submit_attestation()` läser → ingen divergens mellan visad och sparad text; deltagaren kan aldrig attestera med ändrad lydelse. `attestations`-raden lagrar den ordagranna texten + `attested_at` (versionsmarkör i texten: "Lydelse v1, 2026-07"). Ny lydelse = ny version; redan sparade attestationer behåller sin text. [Framåt: per-tenant konfigurerbar lydelse — v1 = en typ, `live_session_honor`.]

### Publik `/verify/<slug>` — smal lookup, ingen bred anon-SELECT
- **`verify_certificate(slug)` (SECURITY DEFINER, grant anon)** returnerar EN certifikats innehav/kurs/utfärdare/status + `brand_spec` för tenant-brandad rendering — samma mönster som `tenant_public_brand` (fynd 4). Inga persondata utöver namnet (inga `user_id`/`email`; regressionstestat). `brand_spec` exponeras redan publikt via `tenant_public_brand` (landningssidan) → ingen ny yta. Revokerat certifikat visas som `revoked`; PDF-vägen ger 404 för revokerat (en återkallad handling kan inte spridas som giltig).
- Sidan `/verify/[slug]` ligger UTANFÖR `[tenant]`-trädet och sätter brand-tokens via samma `parseBrand`+`brandCssVars` (context-lagret återanvänt) → ett context-lager oavsett rutt.

### PDF-verktyg — BESLUT: `@react-pdf/renderer`
- **Valt framför Playwright-print.** Certifikatet utfärdas **on-demand i serverless-runtimen** (Vercel-målet); Playwright hade krävt en chromium-binär vid varje request. react-pdf är ren JS. **å/ä/ö-testet PASSERAT:** "Åsa Öhman, Märsta" + "Utfärdat/uppfyllt" renderas korrekt (PDF inläst okulärt, båda brands). PDF:en byggs ENBART ur `brand_spec` (färger exakt) + `certificate_title` + holder + verify-URL — inget varumärke hårdkodat; **samma komponent, två brands** bevisat (MG Vellum/Ink vs Andningskursen sand/petrol).
- **v1-begränsning (loggad):** PDF-standardfamiljerna (serif→Times, sans→Helvetica) via WinAnsi bär å/ä/ö; brandens EGEN typografi (Lora/Fraunces) via inbäddad TTF är en senare höjning. Brand-IDENTITETEN bärs av färgerna, som återges exakt. (Samma slags medvetna avsteg som next/font-begränsningen i fas 2.)
- **`holder_name`-källa:** `auth.users.raw_user_meta_data->>'full_name'` (seedat fiktivt; Anna = "Anna Öberg" med å/ä/ö så breathworks-flödets riktiga certifikat exercisar svenska tecken). Vid go-live mappas detta till det riktiga registreringsfältet.

### GRIND 5 kvitto (kritisk — breathworks-regressionen = stående grind fr.o.m. nu)
- **`test:certification` 7/7 (ny STÅENDE regressionsvakt):** villkorstrion (`sections_complete` + `final_quiz_pass{80}` + `attestation{live_session_honor}`) som ENSAMma blockerare (var och en saknad → utfärdande NEKAT) · 79 % på slutprovet räcker inte (tröskel 80) · alla tre uppfyllda → certifikat utfärdas + idempotent · deltagare kan inte fejka certifikat (RLS) · publik `/verify` anonymt med rätt data + inga persondata utöver namn + revokerat visas revokerat. **Hermetisk** (engångsdeltagare via service role, raderas med kaskad → rör aldrig demo-tillståndet).
- **Hela tenant #1-flödet klickbart (skarpt i webbläsaren):** login (Anna) → kurs → **intyg-vy (kravtrion, två KLART + attestation KVARSTÅR)** → attestation (versionerad lydelse) → alla tre KLART → **utfärda certifikat** → certifikatkort "Anna Öberg" → **publik verifieringssida (GILTIGT)** → **PDF** i Andningskursens brand. Skärmdumpar + båda-brands-PDF:er i `outputs/kursmotorn/grind5/`. (Avbockning/prov-klickbarheten bevisades i GRIND 3/4; Annas sektioner+slutprov förseedades så cert-stegen kunde köras rent.)
- **Stående vakter omkörda mot ren DB:** RLS-svit **10/10** · test:gating **5/5** · `supabase db reset` + import rent (migration `..09` applicerar från noll) · build grön · hemlighetsgrind **0** · namnbytes-grep **0** · inga priser/procent i `src/`.

### Öppet in i fas 6
- Signerade URL:er för uppladdad media (admin/verify-visning) — INTE byggt i fas 5 (ej i GRIND 5-kriterierna; `recordings`-bucketen är privat, storage-RLS gatar). Byggs när admin-vyn (fas 6) behöver visa inspelningar. · Kohorter/enrollments-flytt + certfönster-omräkning (fas 6, §4.3) · brand-wizard · **breathworks-regressionen (fas 5-sviten) körs IGEN vid GRIND 6 + 7** (grinden mellan motorkärna och MG-tillägg). · Fjärr-DB-synk vid visningsbar milstolpe (Lisas push-OK) — fjärren ligger kvar på fas 2-läget; alla fas 3–5-migrationer + import väntar på synk. · [L] kursens ~27k-ords kvalitetsgrind före riktiga deltagare (go-live-grinden).

## 2026-07-30 — Fas 6: kohorter, aktivitetslogg, admin — migration `..10`

### Serverfunktioner (skrivvägarna för enrollments + logg)
- **`create_enrollment` / `move_enrollment` = SECURITY INVOKER.** RLS (`en_write` = admin) är auktoriteten; funktionerna ger atomik + regelverk (default-starts_at = kohortens start_date; flytt endast aktiv rad, samma kurs + tenant). Uttrycklig `is_tenant_admin`-check i funktionskroppen som bälte-och-hängslen (tydligt fel i stället för tyst 0-raders-update). **Flytt = ny rad** med `moved_from_enrollment_id`, gamla `dropped`; `company`/`price_override_sek` följer med (personens överenskommelse), **invoiced_at/paid_at följer INTE med** (fakturamarkeringar hör till sin enrollment — historiken består på gamla raden). Dubblettaktiv avvisas av det partiella indexet (verifierat via både direkt-API och funktion).
- **`log_activity` = SECURITY DEFINER + `al_insert_own` BORTTAGEN** — deltagarens enda skrivväg till activity_logs (samma mönster som prov-motorn, fas 4): typregistret (log_type_defs) kan inte kringgås, dagtyper (`daily_unique`) dedupas per kurs+dag i funktionen med det partiella practice_day-indexet som DB-backstopp; `cohort_id` härleds ur aktiv enrollment. Ingen datumbegränsning i fas 6 — MG:s 7-dagars-bakåtregel är fas 7-config, inte motorkärna.
- **`tenant_member_names`** — smal SECURITY DEFINER-lookup (namn/e-post ur auth.users för admin-vyerna), admin-gated INUTI funktionen; samma mönster som tenant_public_brand. Fas 5-lärdomen tillämpad: `revoke execute … from public, anon` på alla fyra nya funktioner.

### Rättelse: drip-ankaret (Coworks skiss-antagande var fel — till fas 9-dagboken)
`getGatingState` ankrade scheduled-drip på `enrollment.starts_at`. Datamodellens omankring 2026-07-11 säger **kohortens start_date** (enrollment.starts_at är CERTFÖNSTRENS ankare, inte dripens — en eftersläntrare följer kohortens schema men får egna certfönster). Rättat i `course.ts`; integrationstestat (två kohorter, olika startdatum ⇒ olika upplåsning för respektive deltagare).

### Lärdom: composite-FK:n gör enrollments↔cohorts dubbelrelaterad i PostgREST
Migration `..05` (integritetshärdningen) skapade en andra relation mellan enrollments och cohorts ⇒ **alla embeddar mellan tabellerna kräver FK-hint** (`cohorts!enrollments_cohort_id_fkey(...)`), annars returnerar PostgREST tvetydighetsfel som supabase-js visar som tom data. Bet oss två gånger (kursvyn + admin-kohortlistan) innan mönstret sågs. Regel framåt: composite-FK för integritet ⇒ granska samtliga embeddar mellan tabellparet i samma commit.

### Fönsterberäkningen + underlaget som rena lib-funktioner
- **`log-windows.ts`**: generisk fönsterberäkning (weeks×7 dagar från starts_at, min-dagar per fönster, dispens per window_index) som REN TS-funktion — samma mönster som gating.ts. Fas 6 bygger och testar kärnan (inkl. flytt-omräkningen med loggdagar före/efter — ACCEPTANCE §Fas 6); fas 7 kopplar MG-configen (5/6-veckorsvärdena bor i course_certificate_requirements.config, INTE i kod) + dispens-approvals + certstatus.
- **`underlag.ts`**: effektivt pris (`price_override ?? kohortpris`, ingen tredje fallback), summa, CSV-byggare. Vyn, print-HTML:en och CSV-routen läser ALLA samma `buildUnderlag` ⇒ "CSV = vyn" håller per konstruktion. **Dropped-rader visas (historik) men summeras inte.** Inga delnings-/procentfält någonstans i motorkärnan (regressionstestat i CSV:n). Statusetiketter på svenska i vy + CSV + print (`statusLabel` — UI-copy-regeln; DB-värdena är oförändrat engelska).

### Admin-UI (motor-chrome, aldrig tenant-brand)
`/admin/[slug]` = deltagarlista med kohortfilter (progress %, senast aktiv, slutprovstatus, härledd status) · `/kohorter` CRUD (ingen delete — status `cancelled` är v1:s väg; medvetet scope) · `/kohorter/[id]` = redigering + enrollments + flytt-dialog **med fönstervarning** (egen dialog, ingen browser-confirm) + underlag med CSV/print · `/brand` = brand-wizard. **Härledd status:** "fast" = ingen aktivitet på 14 dagar (`STUCK_AFTER_DAYS`, kodkonstant — v1-heuristik, görs konfigurerbar om en tenant ber om det); "klar" = certifikat eller alla sektioner kompletta.
- **Brand-wizard:** fälten = mallens JSON-schema (`system/templates/brand-spec.md`) + `colors.soft` (motorns soft-badge sedan GRIND 2) + `display_name` (= tenant_name, parseBrands visningsalias). Hexvalidering per token; gissar aldrig fram värden.

### GRIND 6 kvitto
- **Kohortsviten `test:cohort` 7/7 (ny):** create_enrollment default-starts_at · två kohorter olika startdatum ⇒ olika drip-upplåsning samma innehåll · log_activity (okänd typ avvisas, dagtypsdubblett avvisas, guide_session dubblerar, cohort_id härleds) · flytt (ny rad + moved_from + dropped + admin-gated + **fönsteromräkning med loggdagar före/efter**) · dubblettaktiv avvisas (direkt-API + funktion) · fakturerad/betald sätts och ångras · grundarkohorten (11 500 kr, platform/platform, ingen split-rad). Hermetisk (egen kurs + engångsdeltagare, kaskadstädas).
- **Fönster-/underlagssviten `test:windows` 7/7 (ny):** 5/5/5/5/5/5 ✓ · 5/5/4/5/5/5 ✗ · dispens fönster 3 ✓ · eftersläntrarfönster ankras i starts_at · flytt-omräkning · 8 deltagare med en override ⇒ rätt radpris + summa · dropped/CSV=vyn/inga delningsfält.
- **RLS-sviten utökad 10→11 och omskriven för nya skrivvägar** (log_activity i stället för direktinsert — direktinsert regressionstestas som NEKAD; enrollment-funktionerna admin-gated): **11/11**. · **Breathworks-regressionen (fas 5-sviten) 7/7 OFÖRÄNDRAD** — kohortfälten ändrade ingenting för tenant #1 (grinden mellan motorkärna och MG-tillägg). · test:gating 5/5.
- **Skarpt i webbläsaren** (`scripts/grind6-screenshots.mjs`, PNG:er i `outputs/kursmotorn/grind6/`): login → tenants → deltagarlista (kohortfilter klickat) → kohort skapad via UI → kohortdetalj/underlag → flytt-dialog med fönstervarning → fakturerad markerad + ångrad → print-vy → brand-wizard. **Brand-wizardens sparväg bevisad end-to-end:** accent ändrad via UI (#A8894B→#0B5563), `--t-accent` i renderad publik HTML följde med, återställd exakt; CSV-routen verifierad i inloggad session.
- **Statiska vakter mot ren DB:** `supabase db reset` + import rent (migration `..10` från noll) · build grön · lint rent (två kvarhäng från fas 3/5 städade) · hemlighetsgrind **0** · namnbytes-grep **0** · inga priser/procent i `src/`.

### Öppet in i fas 7
- **Fynd 5 (fas 0–1-reviewen) NU AKUT:** practice_day-indexet är globalt per user (user_id, log_type, logged_date) — en deltagare i TVÅ kurser med praxislogg blockeras tvärkurs. `log_activity` dedupar per kurs men indexet är hårdare. Bekräfta avsikt/migrera indexet INNAN MG-fönstren byggs. · [L] MG:s meditationsljudfiler (auto-loggning) · [L] FFMQ-källfil + användarvillkor · Stilla kraft-tokens in via brand-wizard/tenant_brands · signerade URL:er för uppladdad media (när admin-vyn ska visa inspelningar) · fjärr-DB-synk (fjärren på fas 2-läget; migrationer 07–10 + import väntar — Lisas push-OK) · kohort-delete finns inte (status cancelled är vägen; byggs om behov uppstår).

## 2026-07-30 — Fas 7: MG-tenanten fas A — migrationer `..11` + `..12`

### Lisas beslut i fasen (båda tagna i sessionen 2026-07-30)
- **Fynd 5 AVGJORT: practice_day-unikheten scopas PER KURS.** Indexet migrerat till (user_id, course_id, log_type, logged_date) — avsteg från datamodellens ursprungliga indexdefinition, motiv: en deltagare i två kurser med praxislogg (MG + breathworks på samma konto) fick inte blockeras tvärkurs. `log_activity` och indexet säger nu samma sak.
- **MG:s uppladdningar LÅSER INTE nästa vecka.** Kursen är "online i egen takt" (leveransbeskedet 2026-07-29); gating-kärnan fick ett generiskt `optional`-begrepp (sektion blockerar inte sekvensen men förblir oavslutad) och MG:s nio uppladdningssektioner är `upload_required + optional`. Certvillkoret `upload_sections` (minst 1 per sektion V1–V9) spärrar certifikatet tills allt är uppladdat — kravet flyttar aldrig, bara låsningen.

### Motorkärna (migration `..11`)
- **`log_activity` v2:** framtida datum avvisas · manuell loggning begränsas av `log_type_defs.manual_window_days` (nytt generiskt konfigfält; MG praxis = 7 dagar, guide_session = obegränsat) · dagtypsdubblett med source `auto` är **idempotent** (två spelade meditationer samma dag ⇒ EN rad, inget fel — §3.4), manuell dubblett ger fel som UI:t visar lugnt.
- **`log_type_defs.config` (jsonb):** typens UI-konfig — guidesessionens formatlista (A Mikropausen/B/C/annat, kursens egna ord ur C-FINAL v9) + licensmålet (10) bor i DB, inte i motorkod. Namnbytes-säkerheten omfattar även formatnamn.
- **`sections.media_path` + `media_duration_sec`** förberedda för auto-loggningen — **[L] ljudfilerna väntar på Lisa;** spelaren + ≥90 %-mätningen byggs när de finns (auto-idempotensen i log_activity är redan testad).

### Certvillkor (migration `..12`) — kravutvärderingen komplett
- `_cert_requirement_met` utökad med **log_threshold** (fönster från AKTIVA enrollmentens starts_at; dispens via approvals {window_index}; annat ankare än enrollment_starts_at ⇒ fail-closed), **upload_sections** (minst min_per_section i VAR OCH EN av sektionerna; tom lista ⇒ fail-closed) och skärpt **manual_approval** (matchar configens approval_type). Fortsatt EN funktion för status + utfärdande (fas 5-beslutet). MG-certifikat kan ändå inte utfärdas i fas A — manual_approval kräver godkänd bedömning och bedömnings-UI:t är fas B (grindat).
- **TS-fönsterfunktionen (log-windows.ts) är VISNINGENS motor, plpgsql är UTFÄRDANDETS.** Divergensrisken hanteras med samma testmönster mot båda (5×6 · 5/5/4/5/5/5 · dispens · flytt) — test:windows (TS) + test:mg (DB) kör samma fall.
- **Certifikat överlever deltagarradering som REVOKERAD handling:** certificates.user_id nullable + on delete set null (var cascade). GDPR-raderingen revokerar + **anonymiserar** (holder_name → "Raderad deltagare") före kontoraderingen — en utfärdad handling ska kunna slås upp som återkallad, utan persondata.

### MG-import + Stilla kraft
- **Parser `parse-mg-course.mjs`** (veckoformatet: `# Vecka N` + `##`-sektioner) med **inbyggd verbatim-garanti**: rebuildWeek(parseWeek(md)) måste vara tecken-identisk med källfilen, annars kastar importen. `npm run import:mg` importerar C-FINAL v1–9 (183 sektioner) + lägger en uppladdningssektion sist per vecka (V1–V9) + skriver MG:s fyra certvillkor med de riktiga sektions-id:na. **test:mg återuppbygger källfilerna FRÅN DB** — ordagrannheten är regressionstestad, inte stickprovad.
- **Stilla kraft ersätter Editorial Lugn i seed/tenant_brands** (Lisas beslut 2026-07-24): bg=canvas #FAF8F5 · card=paper · primary=navy-900 #0F2647 · primary_dark=navy-950 #081B33 · text=#0F2647 · muted=slate-600 #5B6676 · accent=lilac-600 #715FC1 · soft=stone-200 #E9E4DE · Newsreader/Manrope (tillagda i fontregistret; mono=Manrope — designsystemet saknar mono, Manrope bär data/etiketter). **WCAG AA kontrollerad: alla kombinationer ≥4.5:1** (lägst muted på canvas 5.49). Regressionstest: inga Editorial Lugn-värden kvar i MG:s brand_spec. Logotypfilerna (assets/) väntar på asset-hantering — ordmärke som text tills vidare.
- **Läsvyn `/kurs/vecka/[moduleId]`** (react-markdown + remark-gfm, NY dependency — lokal rendering, inga externa anrop): sektionsinnehåll ordagrant, tenant-tokens via .prose-t, låsta sektioner visas som lugn platshållare. **Tenant-admin får granskningsläge** (allt upplåst, läsning utan avbockning) — generiskt motorbeteende: kursägaren förhandsgranskar sin kurs.

### Deltagarvyer + admin
- **Praxisloggen `/kurs/logg`:** "X av 5 den här veckan", sex fönster som stilla punktrad, manuell loggning (datum + valfri anteckning; servern vaktar 7-dagarsregeln), certstatus per krav (certificate_status-RPC:n — samma DB-sanning som utfärdandet). Ton per §3.1: kvitto, aldrig streak-ångest.
- **"Min guideresa" `/kurs/guideresa`:** formulär (datum/format ur config/sammanhang/antal/anteckning, flera per dag OK), räknare mot licensmålet — **sessioner räknas när logged_date > datumet för första uppladdningen i kursens SISTA uppladdningssektion** (fasen härleds vid läsning, lagras aldrig — §3.2); nivån härleds ur certificates i fas A (mg_guide_status-hooken är fas B). Adminvyn = kolumn "Guidesessioner" i deltagarlistan (fas A-placeringen; guidepipeline-vyn är fas B).
- **GDPR-raderingen:** admin-deltagarlistan har "Radera …" (skriv-RADERA-dialog); servern kräver **plattformsadmin**. `deleteParticipantData` (delad lib, testad direkt): certifikat anonymiseras + revokeras → Storage-objekt under tenant/user-prefixet raderas → auth-kontot raderas (DB-kaskaden tar resten). Service-nyckeln används HÄR för första gången i appkod — endast i denna serveråtgärd, aldrig i renderingsvägen. Obs: raderingen tar hela auth-kontot (alla tenants) — rätt för GDPR-radering av en person.
- **Inloggningssömmen (kvittens):** MG-tenantens stabila inloggnings-URL är **`/mindfulnessguiden/login`** (path-routningen från fas 2). När Lisa verifierat den byter hon `NEXT_PUBLIC_KURSMILJO_URL` i Mindfulnessguiden.se-repot; sajtens egen Supabase-auth behålls enbart för självtestet. Deltagaridentiteten bor i Kursmotorn.
- **Granskningskontot:** `granskning@mind.test` (fiktivt, lösenord = seedens standard), admin-medlem ENBART i MG-tenanten ⇒ granskningsläge i läsvyn. Lisa kan även använda sitt eget konto (owner).

### GRIND 7 kvitto
- **test:mg 9/9 (ny svit):** import ordagrann (DB→källfil, tecken för tecken, alla 9 veckor) · nio uppladdningssektioner + optional + Stilla kraft-tokens (inga Editorial Lugn-rester) · auto-idempotens (två spelningar ⇒ EN practice_day; guide_session dubblerar) · manuell 7-bakåt OK/8 avvisas/framtid avvisas/källan synlig för admin · fönsterberäkningen mot DB-sanningen (5×6 ✓ · 5/5/4/5/5/5 ✗ · dispens fönster 3 ✓ · kohortankrade dagar uppfyller INTE starts_at-fönstren — sen-enrollment-ankaret bevisat) · upload_sections (nio versioner i EN sektion räcker inte; en per sektion uppfyller) · guidesessioner före inspelningen räknas inte/efter räknas · granskningskontot läser 9 veckor/183+ sektioner · GDPR-raderingen (kaskad + Storage + revokerat/anonymiserat certifikat, verify visar "revoked"/"Raderad deltagare").
- **Stående vakter:** RLS-sviten **11/11** · **breathworks-regressionen (fas 5-sviten) 7/7 OFÖRÄNDRAD** — tenant #1 opåverkad av MG-onboardingen (skärmdump 09 bekräftar kursvyn) · test:gating 6/6 (nytt optional-fall) · test:windows 7/7 · test:cohort 7/7 (datumjusterat för framtidsregeln) · `supabase db reset` + BÅDA importerna rent · build + lint gröna · hemlighetsgrind **0** · namnbytes-grep **0** i `src/`.
- **Skärmdumpar** (`scripts/grind7-screenshots.mjs` → `outputs/kursmotorn/grind7/`): granskningskontot i Stilla kraft (kursvy + vecka 1 + vecka 9 läsbara) · Cecilias kursvy/praxislogg (loggad dag, lugn ton) /guideresa (session loggad) · admin-MG med guidekolumn · Andningskursen oförändrad.

### ÖPPET i fas 7 (blockerar inte grinden — [L]-momenten väntar på Lisa)
1. **FFMQ-15 pre (HELA momentet):** väntar på (a) källfilen i mindfulnessguiden.se-kodbasen — items/scoring kopieras ordagrant, byggs ALDRIG fritt; (b) **användarvillkoren för kommersiell app är overifierade** (godkända specen §11.8) — Lisa kontrollerar före färdigbygge. Tidslåsregeln (pre låses när fönster 2 börjar; post nekas före fönster 6) implementeras när formuläret byggs; mg_ffmq_responses-tabellen + RLS + raderingskaskaden är redan på plats och testade.
2. **Auto-loggningen (spelaren):** väntar på MG:s meditationsljudfiler. Mätkontraktet är bestämt: spelaren rapporterar spelad andel av `sections.media_duration_sec`; ≥90 % ⇒ `log_activity(source: auto)` (idempotent, redan testad). Tröskeln 90 % = konfigval [Trolig], sätts skarpt när spelaren byggs.
3. Fjärr-DB-synk: fjärren ligger kvar på fas 2-läget (migrationer 07–12 + båda importerna + nya seeden väntar) — **Lisas uttryckliga push-OK krävs** (gäller oförändrat).
